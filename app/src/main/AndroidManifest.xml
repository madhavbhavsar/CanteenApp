<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.madapps.canteen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity android:name=".owner.packagebtn1.Btn1Activity"></activity>
        <activity android:name=".owner.itemmaster.ItemsActivity" />
        <activity android:name=".owner.workeractivity.WorkerActivity" />
        <activity android:name=".owner.facultyactivity.FacultyActivity" />
        <activity android:name=".owner.dashboardactivity.DashboardActivity" />
        <activity android:name=".worker.entryactivity.EntryActivity" />
        <activity android:name=".loginactivity.LoginActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>